import 'package:flutter/material.dart';

class ChangeIconAnimation extends StatelessWidget {
  final AnimationController controller;
  final Animation<double> digre;
  const ChangeIconAnimation(
      {Key? key, required this.controller, required this.digre})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
